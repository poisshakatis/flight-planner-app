## Kuidas käivitada

### Installi sõltuvused

Võid kasutada ühte neist: `npm`, `yarn`, `pnpm`, `bun`. Näide kasutades `npm`:

```bash
npm install
```

### Käivita arendusserver

```bash
npm run dev
```
